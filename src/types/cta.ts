// Tipo da chamada para ação final da página

export interface CTA {
  eyebrow: string;
  title: string;
  description: string;
  primaryLabel: string;
  primaryHref: string;
  destination: string;
}
